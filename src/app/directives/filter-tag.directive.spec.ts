import { FilterTagDirective } from './filter-tag.directive';

describe('FilterTagDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterTagDirective();
    expect(directive).toBeTruthy();
  });
});
